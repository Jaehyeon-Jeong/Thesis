# Stage 3 Source

## English

Stage 3 source code will be added after the planning checklist confirms the
Linear adapter insertion point and feature dimensions.

Expected modules:
- model wrapper for `CNN + Linear(bias=False)`,
- runner utilities that reuse Stage 2 datasets and metrics,
- Grad-CAM comparison utilities for baseline vs Linear.

## 한국어

Stage 3 source code는 planning checklist에서 Linear adapter 삽입 위치와 feature
dimension을 확인한 뒤 추가합니다.

예상 module:
- `CNN + Linear(bias=False)` model wrapper,
- Stage 2 dataset과 metric을 재사용하는 runner utility,
- baseline과 Linear model Grad-CAM을 비교하는 utility.
