# Stage 5 Configs

Config files will be added after `5-1` locks the first embedding backend.

Expected settings:

- news source path
- embedding backend: `openai` or `local`
- embedding model id
- embedding dimensions
- window sizes: `7`, `20`, `60`
- daily lag policy
- SVD/PCA dimensions
- Stage 2 checkpoint bundle path
- Stage 4/Stage 5 output roots

