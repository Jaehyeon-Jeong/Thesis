# Stage 5 Data Inventory

This folder records small manifests only.

Do not commit large embedding matrices, raw API dumps, large news tables, or
Kaggle bundles.

Expected small files:

- news coverage audit CSV
- embedding input manifest
- embedding input summary and sample pointers
- embedding backend manifest
- cached artifact path list
- train/validation/test coverage summary
